<local:MyHint 
    Text="此主页由 @Deep-Dark-Forest 开发与维护。" 
    IsWarn="False" 
    Margin="0,0,0,15"
/>

<local:MyCard Title="所有状态各标签 Issue 数量" Margin="0,0,0,15" CanSwap="True" IsSwaped="True">
    <StackPanel Margin="25,40,23,15">
        <local:MyHint Text="为避免重复，仅适用于开启/关闭状态 Issue 的 label，其对应的 Issue 数量不会在此处呈现。" IsWarn="False" Margin="0,0,0,10"/>
        
        <StackPanel Margin="0,0,0,15">
            <WrapPanel>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="优先级低" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{AllStatusPriorityLow}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="优先级中" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{AllStatusPriorityMedium}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="优先级高" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{AllStatusPriorityHigh}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="优先级极高" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{AllStatusPriorityCritical}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
            </WrapPanel>
        </StackPanel>

        <StackPanel Margin="0,0,0,15">
            <WrapPanel>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="Bug" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{AllStatusBugCount}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="优化" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{AllStatusOptimizationCount}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="新功能" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{AllStatusFeatureCount}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
            </WrapPanel>
        </StackPanel>

        <StackPanel>
            <WrapPanel>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="尝鲜特性" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{AllStatusExperimentalCount}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="第三方" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{AllStatusThirdPartyCount}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="等待提交者" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{AllStatusAwaitingSubmitterCount}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="需要社区帮助" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{AllStatusCommunityHelpCount}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="需要社区复现" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{AllStatusCommunityReproductionCount}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
            </WrapPanel>
        </StackPanel>
    </StackPanel>
</local:MyCard>

<!-- 开启状态卡片（结构相同，仅展示第一个区块） -->
<local:MyCard Title="开启状态各标签 Issue 数量" Margin="0,0,0,15" CanSwap="True" IsSwaped="True">
    <StackPanel Margin="25,40,23,15">
        <StackPanel Margin="0,0,0,15">
            <WrapPanel>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="处理中" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{OpenStatusInProgressCount}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <!-- 其他开启状态区块同理... -->
            </WrapPanel>
        </StackPanel>
    </StackPanel>
</local:MyCard>

<!-- 关闭状态卡片（结构相同，仅展示第一个区块） -->
<local:MyCard Title="关闭状态各标签 Issue 数量" Margin="0,0,0,15" CanSwap="True" IsSwaped="True">
    <StackPanel Margin="25,40,23,15">
        <StackPanel Margin="0,0,0,15">
            <WrapPanel>
                <Border Margin="5" Background="Transparent" BorderBrush="{DynamicResource ColorBrush4}" BorderThickness="1" CornerRadius="4" Padding="8" Width="90" Height="70">
                    <Grid>
                        <Border Background="{DynamicResource ColorBrush4}" CornerRadius="2" Padding="4,2" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,-10,0,0">
                            <TextBlock Text="完成" FontSize="10" Foreground="White" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{{ClosedStatusCompletedCount}}" FontSize="22" FontWeight="SemiBold" Foreground="#FF333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Grid>
                </Border>
                <!-- 其他关闭状态区块同理... -->
            </WrapPanel>
        </StackPanel>
    </StackPanel>
</local:MyCard>

<!-- 操作面板卡片保持不变 -->
<local:MyCard Title="操作面板" Margin="0,0,0,15" CanSwap="False" IsSwaped="True">
    <StackPanel Margin="25,20,23,20" Orientation="Horizontal" HorizontalAlignment="Center">
        <local:MyButton 
            Margin="0,0,10,0" 
            Width="180" 
            Height="35" 
            Padding="15,0,15,0"
            ColorType="Highlight"
            Text="刷新" 
            EventType="刷新主页"
            ToolTip="从云端获取最新数据（不要频繁点击）"
        />
        <local:MyButton 
            Margin="0,0,10,0" 
            Width="180" 
            Height="35" 
            Padding="15,0,15,0"
            Text="GitHub 仓库" 
            EventType="打开网页" 
            EventData="https://github.com/Deep-Dark-Forest/PCL2-GitHub-Dashboard-Homepage"
            ToolTip="访问本主页的 GitHub 仓库"
        />
    </StackPanel>
</local:MyCard>
